# API base URL (no trailing slash).
# Leave empty in dev to use Vite proxy (/api). Set in production for Static Site deploy.
VITE_API_URL=

# Production examples:
# Same-origin (backend serves frontend): leave empty or set to same host
# Separate Static Site: point to your backend Web Service
# VITE_API_URL=https://your-backend-name.onrender.com/api
