# Server
PORT=5000
NODE_ENV=development

# Database (required for subscribe & contact)
DATABASE_URL=postgresql://user:password@localhost:5432/fitchef

# CORS: allowed frontend origin (use your Render frontend URL if deploying separately)
CLIENT_ORIGIN=http://localhost:5173

# Admin API protection
ADMIN_API_KEY=your-secure-random-api-key-here
JWT_SECRET=your-jwt-secret-for-future-auth

# Email (optional – signup notifications to bodybank.fit369@gmail.com)
# Use Gmail + App Password, or EMAIL_USER / EMAIL_PASS
MAIL_USER=your-gmail@gmail.com
MAIL_APP_PASSWORD=your-16-char-app-password
# Alternative names (same purpose):
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASS=your-app-password
